# Campaign Saves

This folder is for optional exported campaign save JSON files.

Runtime Campaign Mode can save to browser storage, export/import JSON, fork a local slot, or push a save file to GitHub through `SaveManager.saveTextFileToGitHub(path, text)`.

Do not put authored campaign definitions here. Authored content belongs under `data/campaigns/`.
